name: Broken Workflow
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      - name: Install
        run: npm install
      - name: Test
        run: npm test
      - name: Faulty Step
        run: echo "This will cause a YAML syntax error
